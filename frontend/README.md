# Frontend (Autograding2026)

React + TypeScript + Vite. Talks to the backend (see `../backend`) via
`src/services/api.ts`; `vite.config.ts` proxies `/api` to
`http://localhost:8000` in dev.

## Run locally

```bash
cd frontend
npm install
npm run dev
```
